"""Permanent mutation matrix for the confirmatory scientific identity.

This package is an *audit instrument*, not part of the scientific surface.  It
materializes a target tree, applies a fixed catalog of mutations to disposable
copies of it, measures the identity before and after each one in an isolated
subprocess, and compares what it observes against the effect the catalog
demands.

Nothing here is imported by ``app/``.  The authorization to use attribute
reassignment, dynamic import and ``sys.modules`` surgery is scoped to this
directory and to the probe it injects: reproducing a runtime escape requires
performing it.
"""
